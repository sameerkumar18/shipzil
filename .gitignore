.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
dist/
build/
*.egg-info/
.probe/

# Scraped provider API docs. Reference only, never committed.
.apidocs/

# Built docs site. `mkdocs build` output, never committed.
site/

# Local build playbook — tricks, scrape recipes, credential inventory, open
# operational state. Deliberately never pushed. Must not move into docs/, since
# MkDocs builds everything under there.
NOTES.local.md
*.local.md
